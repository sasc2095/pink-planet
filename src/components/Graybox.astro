
<article class="graybox">
    <slot/>
    </article>

    <style>
        .graybox{
            margin: 4rem 0;
            padding: 1rem 0 3rem 0;
            background-color: var(--secondary-dgray);
        }
    </style>