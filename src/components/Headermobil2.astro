---

---

<header>
  <div class="login_link">
    <img class="acc" src="../../public/SVG/account_circle.svg" alt="login icon" />
   <div class="container_forms">
        <div class="form-box_login">
          
            <div class="form-content">
                <h2>LOGIN</h2>
                <form action="#">
                    <div class="input-field">
                        <input type="text" required>
                        <label>Email</label>
                    </div>
                    <div class="input-field">
                        <input type="password" required>
                        <label>Adgangskode</label>
                    </div>
                     <button type="submit">Log In</button><br>
                    <a href="#" class="forgot-pass-link">Forgot password?</a>
                   
                </form>
                <div class="bottom-link">
                    Don't have an account?
                    <a href="#" id="signup-link">Signup</a>
                </div>
            </div>
        </div>
  
</div>
  </div>

  <div class="burger_nav">
    <div class="nav_bar">
      <a class="nav_link" href="/">Forside</a>
      <a class="nav_link" href="/signup_membership">Medlemsskab sign up</a>
      <a class="nav_link" href="/membership_page">Medlemsskab</a>
      <a class="nav_link" href="/info">Information</a>
    </div>
  </div>
  <a href="../pages/index" class="bcmLogo">
    <img src="../../public/SVG/logo.svg" alt="Borup Cykel Motion logo" />
  </a>
  <section class="burgermenu">
    <span class="line_1"></span>
    <span class="line_2"></span>
    <span class="line_3"></span>
  </section>
</header>

<script>
/* login form */
  const activatePopup = document.querySelector(".login_link");
  const showPopup = document.querySelector(".container_forms");
  const formBox = document.querySelector(".form-box_login")


// Show login popup
activatePopup.addEventListener("click", () => {
    showPopup.classList.toggle("active");
    formBox.classList.toggle("active");
});


showPopup.addEventListener("click", removePopup);

function removePopup(){
  showPopup.classList.remove("active");
      formBox.classList.remove("active");
}

  /* Burgermenu */
  const burgerMenu = document.querySelector(".burgermenu");
  const navMenu = document.querySelector(".burger_nav");
  const marginActive = document.querySelector("main");

  burgerMenu.addEventListener("click", () => {
    burgerMenu.classList.toggle("active");
    navMenu.classList.toggle("active");
    marginActive.classList.toggle("active");
  });

  document.querySelector(".nav_link").addEventListener("click", remove);

  function remove() {
    burgerMenu.classList.remove("active");
    navMenu.classList.remove("active");
    marginActive.classList.remove("active");
  }
</script>

<style>
  header {
    background-color: #ebd9a6;
    display: grid;
    grid-template-columns: 1fr 3fr 1fr;
    align-items: center;
  }
  .bcmLogo {
    margin: auto;
    width: 40%;
    height: auto;
  }
  .acc {
    margin: auto;
    width: 50%;
  }

  /* login form */

  .acc:hover{
  background-color:#d38644;
  border-radius: 100%;
   padding:1rem;

  }

  .form-content{
        width: 100%;
    padding: 2rem;

  }


  form .input-field {
    position: relative;
    height: 50px;
    width: 100%;
    margin-top: 20px;
}
.input-field input {
    height: 100%;
    width: 100%;
    background: none;
    outline: none;
    font-size: 0.95rem;
    padding: 0 15px;
    border: 1px solid #717171;
    border-radius: 3px;
}
.input-field input:focus {
    border: 1px solid #d38644;
}
.input-field label {
    position: absolute;
    top: 50%;
    left: 15px;
    transform: translateY(-50%);
    color: #4a4646;
    pointer-events: none;
    transition: 0.2s ease;
}
.input-field input:is(:focus, :valid) {
    padding: 16px 15px 0;
}
.input-field input:is(:focus, :valid)~label {
    transform: translateY(-120%);
    color: #d38644;
    font-size: 0.75rem;
}

  .container_forms{
    display:none;
  }


  .container_forms.active{
    display:block;
    position:absolute;
    z-index: 4;
    width: 25rem;
    height:auto;
    background-color:#2a3823;
    color:white;
    border-radius:5px;
    padding-bottom: 0.5rem;
  }
 button {
    background-color: #d38644;
    border-radius: 5px;
    text-align: center;
    text-decoration: none;
    border: none;
    color:white;
    display: inline-block;
    padding: 0.5rem 0.5rem;
    margin:1rem 0;
    width:100%
  }

  button:hover{
    text-decoration:underline;
  }

   #signup-link, .forgot-pass-link{
   
    color:#d38644;
    text-decoration:none;
  }

  

  .forgot-pass-link:hover, #signup-link:hover{
    text-decoration:underline;
  }

  
  /************ burger menu ***********/

  .burger_nav {
    display: none;
    top: 6rem;
    right: 0;
  }

  .burger_nav.active {
    display: block;
    position: absolute;
    background-color: #c0a4a4;
    z-index: 4;
    width: 20rem;
    height:100%;
  }

  .nav_bar {
    display: flex;
    flex-direction: column;
  }

  .nav_link {
    padding: 40px 40px 40px 65px;
  }

  span {
    display: block;
    width: 30px;
    height: 3px;
    margin: 5px auto;
    transition: 0.5s ease-in-out;
  }

  .line_1 {
    background-color: rgb(0, 0, 0);
  }

  .line_2 {
    background-color: rgb(0, 0, 0);
  }

  .line_3 {
    background-color: rgb(0, 0, 0);
  }

  .burgermenu.active .line_2 {
    opacity: 0;
  }
  .burgermenu.active .line_2:nth-child(2) {
    opacity: 0;
  }

  .burgermenu.active .line_1:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }

  .burgermenu.active .line_3:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
  }
</style>
